databases:
  - name: yomislo-db
    plan: free
    databaseName: yomislo_production
    user: yomislo

services:
  - type: web
    name: yomislo
    runtime: ruby
    plan: starter
    buildCommand: ./bin/render-build.sh
    startCommand: bundle exec puma -C config/puma.rb
    healthCheckPath: /up
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: yomislo-db
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false
      - key: RAILS_ENV
        value: production
      - key: RAILS_LOG_TO_STDOUT
        value: "1"
      - key: RAILS_SERVE_STATIC_FILES
        value: "1"
      - key: ADMIN_EMAIL
        sync: false
      - key: ADMIN_PASSWORD
        sync: false

  # ── 日次: P-WORLD全店同期（機種リスト+台数を1回のフェッチで取得） ──
  # 03:00 JST (18:00 UTC前日) — 約4時間
  # ※毎月1日はスキップ (Job内判定、月次ジョブが代わりに実行)
  - type: cron
    name: yomislo-daily-refresh
    runtime: ruby
    plan: starter
    schedule: "0 18 * * *"
    buildCommand: ./bin/render-build.sh
    startCommand: >
      bundle exec rails runner "PworldScraper.import_new_machines_from_schedule; DailyMachineRefreshJob.perform_now"
    envVars: &batch-env
      - key: DATABASE_URL
        fromDatabase:
          name: yomislo-db
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false
      - key: RAILS_ENV
        value: production
      - key: RAILS_LOG_TO_STDOUT
        value: "1"

  # ── 日次: 軽量集計バッチ（1つにまとめて $1/月） ──────────
  # ランキング + 収支集計 + VoterProfile — 04:00 JST (19:00 UTC前日)
  # NOTE: SNS収集は一旦停止（有効なフィードが少なく効果薄）
  - type: cron
    name: yomislo-daily-aggregation
    runtime: ruby
    plan: starter
    schedule: "0 19 * * *"
    buildCommand: ./bin/render-build.sh
    startCommand: >
      bundle exec rails runner "
        VoterRanking.refresh_weekly!;
        VoterRanking.refresh_monthly!;
        VoterRanking.refresh_all_time!;
        PlayRecordSummary.refresh_all!;
        Vote.where(voted_on: 7.days.ago..Date.current).select(:voter_token).distinct.pluck(:voter_token).each { |t| VoterProfile.refresh_for(t) }
      "
    envVars: *batch-env

  # ── 月次: P-WORLD全店フル同期（機種+台数+店舗詳細を1回で取得） ──
  # 毎月1日 03:00 JST (前月末日 18:00 UTC) — 約4時間
  - type: cron
    name: yomislo-monthly
    runtime: ruby
    plan: starter
    schedule: "0 18 1 * *"
    buildCommand: ./bin/render-build.sh
    startCommand: bundle exec rails runner "MonthlyShopDetailsJob.perform_now"
    envVars: *batch-env
